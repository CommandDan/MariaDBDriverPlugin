# MariaDBDriverPlugin
Registers a single MariaDB JDBC driver for all plugins.

The Connector version, java version and Paper API version (for 1.21.x) are set in `gradle.properties`.

To build the plugin use the Gradle 'build' task.
